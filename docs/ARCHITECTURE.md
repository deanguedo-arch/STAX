# Architecture

RAX is a local TypeScript CLI. It routes each input through risk scoring, boundary enforcement, prompt stack construction, retrieval, agent execution, critic review, formatter enforcement, validation, and logging.
