# RAX Safety Model

The local risk model scores intent, harm, actionability, privacy, exploitation, regulated advice, and system integrity on a 0-3 scale. Boundary decisions are allow, constrain, refuse, or redirect. Refused requests do not call providers.
