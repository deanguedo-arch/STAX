# RAX Memory Model

Memory is local JSON storage. Raw model output is not saved automatically. Retrieval returns only approved, non-expired memory items. Corrections can become memory only after approval.
