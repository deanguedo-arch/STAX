# RAX Evidence Request Builder Report

`EvidenceRequestBuilder` turns `no_local_basis` into the smallest useful
evidence request instead of a dead-end refusal.

Templates cover repo questions, UI questions, Codex reports, deployment issues,
and runtime claims. The builder never runs commands; it names the evidence to
paste back and one example command when appropriate.
