# Next Actions

1. Run `npm run typecheck` and `npm test`.
2. Run `npm run rax -- eval`, `npm run rax -- eval --redteam`, and `npm run rax -- eval --regression`.
3. Smoke Project Brain, Codex Audit, and mode maturity CLI commands.
