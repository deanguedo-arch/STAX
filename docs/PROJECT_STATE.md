# Project State

STAX/RAX is a local, mock-provider-first governed reasoning runtime. The current objective is to preserve Behavior System v0.1 while adding project governance surfaces: Project Brain, Codex Audit, mode registry, mode maturity, evidence registry, claim ledger, prompt factory, test gap audit, and policy drift detection.

## Current Objective

Implement the next-stage governance modes without changing STAX fitness behavior or enabling unsafe tools.

## Evidence Baseline

- `ev_001`: v0.1 proof report.
- `ev_002`: STAX atomic extraction proof.

## Working Rules

- Do not add UI before CLI stability.
- Do not enable shell or file write tools by default.
- Do not auto-save raw model outputs to memory.
- Do not claim completion without command output.
