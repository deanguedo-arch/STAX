# Project State

STAX/RAX is a local, mock-provider-first governed reasoning runtime. STAX is the adaptive rule-aware learning/runtime system; RAX is the internal runtime engine/name where still used; `stax_fitness` is one explicit optional domain mode.

The current objective is to preserve Behavior System v0.1 while adding the approved learning loop: LearningEvent, queue, proposal, approval gate, metrics, Project Brain, Codex Audit, mode registry, mode maturity, evidence registry, claim ledger, prompt factory, test gap audit, and policy drift detection.

## Current Objective

Implement the STAX approved learning runtime without changing explicit `stax_fitness` behavior or enabling unsafe tools.

## Evidence Baseline

- `ev_001`: v0.1 proof report.
- `ev_002`: STAX atomic extraction proof.
- `ev_003`: STAX identity rule: STAX is the system; `stax_fitness` is an explicit domain mode.

## Working Rules

- Do not add UI before CLI stability.
- Do not enable shell or file write tools by default.
- Do not auto-save raw model outputs to memory.
- Do not claim completion without command output.
- Do not claim autonomous learning; claim approved learning loop only.
