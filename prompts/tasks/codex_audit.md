# Codex Audit Task

Audit the supplied Codex claim. Accept only concrete evidence such as command output, test/eval results, traces, diffs, or artifacts. If evidence is missing, recommend rejection and write a fix prompt.
